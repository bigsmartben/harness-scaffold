rules:
  - paths:
      - services/orders/**
    validation_level: integration
    tasks:
      - integration-suite
      - full-ci
  - paths:
      - services/billing/**
    validation_level: integration
    tasks:
      - integration-suite
      - full-ci
