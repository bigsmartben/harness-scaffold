# Governance contract

The structured projection in `.harness/governance/` is the SSOT. `AGENTS.md` is its runtime entrypoint. G0-G7 and accepted Action Evidence determine whether a governed conclusion is valid.
