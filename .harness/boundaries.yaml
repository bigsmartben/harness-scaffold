write_scope:
  include:
    - "**"
  exclude:
    - .git/**
    - .harness/runs/**
    - .harness/reports/**
    - .harness/cache/**
expansion:
  action: stop-and-handoff
  blocker_codes:
    - WRITE_SCOPE_EXPANDED
    - HANDOFF_REQUIRED

