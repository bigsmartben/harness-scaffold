id: pipeline:merge
kind: merge
requires_independent_confirmation: true
stages:
  - id: contracts
    tasks:
      - test:contracts
    requires_evidence: true
  - id: required-check
    tasks:
      - test:contracts-ci
    requires_evidence: true
  - id: full-ci
    tasks:
      - ci:full
    requires_evidence: true
